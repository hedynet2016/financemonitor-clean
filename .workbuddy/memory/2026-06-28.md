# 2026-06-28 工作日誌

## 本地 Web UI 與 Render 不同步問題排查
- **問題**：本地 Web UI 顯示舊內容，Render 上顯示最新內容
- **根因 1**：Flask `debug=False`（webui.py:1098），服務器不會自動重載代碼。git pull 更新了磁碟上的代碼，但運行中的服務器仍執行舊代碼
- **根因 2**：config.json 在 .gitignore 中，git pull 不會帶來此文件。本地缺少 config.json 導致首頁顯示 "Starting" 等待頁面
- **修復步驟**：
  1. 殺掉舊服務器進程（PID 33812）
  2. 重新啟動 webui.py（--no-browser）
  3. 運行 `scripts/generate_config.py` 生成 config.json
- **驗證**：首頁恢復正常標題，/tasks 頁面顯示最新內容（「經濟指標相關新聞」「涵蓋：BLS / Fed / ECB / BOJ / BOE 等官方機構」）
- **注意事項**：每次 git pull 後需重啟本地服務器才能看到最新更改

## 每日 GitHub 同步（09:00 自動化）
- 已從 `2026-06-24-10-18-32` 複製指定檔案到 `financemonitor-clean`
- 所有檔案內容與目前 repo 一致，無新 commit、無需 push
- 本地 main 分支已與 `origin/main` 同步
